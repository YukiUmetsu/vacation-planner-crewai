# City route crew (placeholder)

Proposes an ordered list of cities and nights when the trip destination is a **country** or **region**.

Not wired yet. Target output: `CityRoute` / `CityStop` (see `agent/models` and `docs/DATA_MODEL.md`).
